<h1><%= @event.name %></h1>

<div>
    <p>Date: <%= @event.date.strftime("%A, %d %b %Y %l:%M %p") %> </p>
    <p>Cost: $<%= @event.cost %> </p>
    <p>Description: <%= @event.description %> </p>
    <p>Image: <img src=<%= @event.image %> > </p>
    <p>Event Types: </p>
    <% @event.event_types.each do |e| %>
        <p><%= e.event_type %>
    <% end %>
    
</div>
